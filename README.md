# APB_GPIO-Module
The apb_gpio module implements an 8-bit GPIO peripheral connected to the APB bus that allows a processor to configure pin direction, mode (push-pull/open-drain), write output values, and read input values through memory-mapped registers.
